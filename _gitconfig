[user]
    email = zhengdi05@foxmail.com
    name = zhengdi

[core]
    filemode = true
    excludesfile = ~/.gitignore
    editor = vim 

[diff]
    renames = copy
    tool = vimdiff

[merge]
    tool = vimdiff

[rerere]
    enabled = true

[color]
    ui = auto

[alias]
    st = status
    ci = commit
    br = branch
    co = checkout
    ls = ls-files
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --

[push]
	default = matching
